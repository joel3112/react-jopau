import {
  SBDescription,
  SBLinks,
  SBPureArgsTable,
  SBSubTitle,
  SBTitle
} from '@react-jopau/shared/stories';

<SBTitle>useLocalStorage</SBTitle>

<SBLinks>
  <SBLinks.Item href="hooks-uselocalstorage--default">Playground</SBLinks.Item>
</SBLinks>

<SBDescription>Persist a value in local storage and keep it in sync with the state.</SBDescription>

```tsx dark
import { useLocalStorage } from '@react-jopau/hooks';
```

<SBSubTitle>Examples</SBSubTitle>

```tsx dark
const [darkMode, setDarkMode] = useLocalStorage<boolean>('key', true);

const handleClick = () => setDarkMode((prev) => !prev);

return <button onClick={handleClick}>{darkMode ? 'Dark mode' : 'Light mode'}</button>;
```

<SBPureArgsTable
  heading="Params"
  rows={{
    initialValue: {
      name: 'initialValue',
      description: 'The initial value to use if there is no value in local storage.',
      required: true,
      type: 'T'
    },
    key: {
      name: 'key',
      description: 'The key to use in local storage.',
      required: true,
      type: 'string'
    }
  }}
/>

<SBPureArgsTable
  heading="Returns"
  noDefaults
  rows={{
    'result[0]': {
      name: 'result[0]',
      description: 'The value of the local storage',
      required: false,
      type: 'T'
    },
    'result[1]': {
      name: 'result[1]',
      description: 'The setter of the local storage',
      required: false,
      type: 'Dispatch.<SetStateAction.<T>>'
    }
  }}
/>
